# Existing public/legacy client used by the current application paths.
SUPABASE_URL=
SUPABASE_KEY=

# Server-only credential for RLS-protected research operations.
# Prefer the current Supabase secret key format. Never expose this to frontend code.
SUPABASE_SECRET_KEY=

# Legacy alternative during migration; leave empty when SUPABASE_SECRET_KEY is used.
SUPABASE_SERVICE_ROLE_KEY=
